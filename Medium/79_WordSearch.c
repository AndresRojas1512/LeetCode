#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

// Not finished

bool starsWith(int r, int c, char **board, int boardSize, int *boardColSize, char *word)
{
    if (strlen(word) == 0) // empty word (no letters)
    {
        return true;
    }
}

bool exist(char** board, int boardSize, int* boardColSize, char * word)
{
    int rows = boardSize;
    int columns = *boardColSize;
}